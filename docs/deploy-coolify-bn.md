# MUTI ওয়েবসাইট — Coolify-তে ডিপ্লয় (A to Z)

এই ফাইলটা ধাপে ধাপে অনুসরণ করলেই সাইট লাইভ হয়ে যাবে। প্রতিটা ধাপ একবার করলেই
হয় — পরের বার কোড আপডেট করলে শুধু ধাপ ১১ লাগবে।

> স্পেসিফিকেশনের সেকশন ১২ এই ডিপ্লয়মেন্টের ভিত্তি। বাকি থাকা কাজের তালিকা
> `HANDOVER.md`-এ।

---

## ০. শুরুর আগে যা যা হাতে থাকতে হবে

| জিনিস                     | বিস্তারিত                                                                                                |
| ------------------------- | -------------------------------------------------------------------------------------------------------- |
| **VPS**                   | Coolify ইনস্টল করা একটা সার্ভার। **কমপক্ষে ২ GB RAM** (৪ GB হলে ভালো) — Next.js বিল্ড কম RAM-এ ফেইল করে। |
| **ডোমেইন**                | যেমন `mutibd.com`। এখনো কেনা হয়নি (HANDOVER ১.১)। পোস্টারে লেখা "MUTI.Ultrasound.com" বৈধ ডোমেইন নয়।   |
| **Cloudflare অ্যাকাউন্ট** | ফ্রি প্ল্যানই যথেষ্ট। ডোমেইনের nameserver Cloudflare-এ পয়েন্ট করা থাকতে হবে।                            |
| **GitHub রিপো**           | `git@github.com:akashsarker3400/muti.git`, ব্রাঞ্চ `main`।                                               |
| **Gmail App Password**    | আবেদনের নোটিফিকেশন ইমেইলের জন্য (ঐচ্ছিক — না দিলে আবেদন সেভ হবে, শুধু ইমেইল যাবে না)।                    |

**Gmail App Password কীভাবে পাবেন:** Google Account → Security → 2-Step
Verification চালু করুন → App passwords → নতুন একটা তৈরি করুন। ১৬ অক্ষরের যে কোডটা
পাবেন সেটাই `SMTP_PASS`। নিজের Gmail লগইন পাসওয়ার্ড **নয়**।

---

## ১. ডোমেইন ও DNS

১. ডোমেইন কিনুন (Namecheap / Porkbun / GoDaddy — যেকোনোটা)।
২. Cloudflare-এ সাইটটি যোগ করুন, Cloudflare যে দুটো nameserver দেবে সেগুলো
ডোমেইন রেজিস্ট্রারে বসিয়ে দিন। প্রোপাগেট হতে কয়েক ঘণ্টা লাগতে পারে।
৩. Cloudflare → DNS-এ দুটো রেকর্ড বানান:

| Type | Name  | Content   | Proxy status                    |
| ---- | ----- | --------- | ------------------------------- |
| A    | `@`   | VPS-এর IP | **DNS only (ধূসর মেঘ)** — আপাতত |
| A    | `www` | VPS-এর IP | **DNS only (ধূসর মেঘ)** — আপাতত |

> ⚠️ **এখনই কমলা মেঘ (Proxied) করবেন না।** Coolify-কে Let's Encrypt থেকে SSL
> সার্টিফিকেট আনতে হবে, আর Cloudflare প্রক্সি চালু থাকলে সেটা আটকে যায়। ধাপ ৯-এ
> সার্টিফিকেট আসার পর কমলা করবেন।

---

## ২. Coolify-তে PostgreSQL সার্ভিস

১. Coolify → **Projects** → নতুন প্রজেক্ট বানান (নাম: `MUTI`)।
২. ভেতরে **+ New Resource** → **Databases** → **PostgreSQL** → ভার্সন **16**।
৩. নাম দিন `muti-db`, **Deploy** চাপুন।
৪. ডিপ্লয় হয়ে গেলে ঐ ডাটাবেসের পেজে **Postgres URL (internal)** লেখা একটা
কানেকশন স্ট্রিং দেখাবে। এটা **কপি করে রাখুন** — পরের ধাপে লাগবে। দেখতে এমন:

```
postgres://postgres:xxxxxxxxxxxx@abcdefgh:5432/postgres
```

> **"internal" স্ট্রিংটাই নিন, "external" নয়।** ইন্টারনাল মানে ডাটাবেস শুধু
> সার্ভারের ভেতর থেকে অ্যাক্সেসযোগ্য — ইন্টারনেটে খোলা থাকে না। ডাটাবেস কখনোই
> পাবলিকলি খুলবেন না।

---

## ৩. অ্যাপ্লিকেশন তৈরি

১. একই প্রজেক্টে **+ New Resource** → **Private Repository (with GitHub App)**
বেছে নিন (রিপো পাবলিক হলে **Public Repository**)।

- প্রাইভেট হলে Coolify-কে GitHub App হিসেবে ইনস্টল করতে বলবে — অনুমতি দিন,
  `akashsarker3400/muti` রিপোটা সিলেক্ট করুন।
  ২. সেটিংসগুলো:

| ফিল্ড               | মান            |
| ------------------- | -------------- |
| Branch              | `main`         |
| **Build Pack**      | **Dockerfile** |
| Dockerfile location | `/Dockerfile`  |
| Base Directory      | `/`            |
| Port(s) Exposed     | `3000`         |

> **Nixpacks নয়, Dockerfile বেছে নিন।** রিপোতে একটা মাল্টি-স্টেজ Dockerfile আছে যেটা
> ইমেজ ছোট রাখে আর চালু হওয়ার সময় নিজে থেকেই মাইগ্রেশন ও সিড চালায়।

৩. **এখনই Deploy চাপবেন না** — আগে ধাপ ৪, ৫, ৬ শেষ করুন।

---

## ৪. Environment variables

অ্যাপের **Environment Variables** ট্যাবে গিয়ে নিচেরগুলো যোগ করুন:

| Variable               | মান                                                  | Build Variable? |
| ---------------------- | ---------------------------------------------------- | --------------- |
| `DATABASE_URL`         | ধাপ ২-এর internal URL, শেষে `?schema=public` যোগ করে | না              |
| `AUTH_SECRET`          | `openssl rand -base64 32` চালিয়ে যা আসে             | না              |
| `AUTH_URL`             | `https://mutibd.com`                                 | না              |
| `NEXT_PUBLIC_SITE_URL` | `https://mutibd.com`                                 | **✅ হ্যাঁ**    |
| `ADMIN_EMAIL`          | `mymensinghultrasound@gmail.com`                     | না              |
| `ADMIN_PASSWORD`       | শক্ত একটা পাসওয়ার্ড (প্রথম লগইনের পর বদলাবেন)       | না              |
| `UPLOAD_DIR`           | `/app/uploads`                                       | না              |
| `SMTP_HOST`            | `smtp.gmail.com`                                     | না              |
| `SMTP_PORT`            | `587`                                                | না              |
| `SMTP_USER`            | `mymensinghultrasound@gmail.com`                     | না              |
| `SMTP_PASS`            | Gmail App Password (১৬ অক্ষর)                        | না              |
| `NOTIFY_EMAIL`         | `mymensinghultrasound@gmail.com`                     | না              |

### গুরুত্বপূর্ণ তিনটা কথা

- **`NEXT_PUBLIC_SITE_URL`-এর পাশে "Build Variable?" চেকবক্সটা অবশ্যই টিক দিতে
  হবে।** এই মানটা বিল্ডের সময় কোডের ভেতর বসে যায়। টিক না দিলে সাইটম্যাপ, canonical
  URL আর সোশ্যাল শেয়ারের ছবিতে `localhost:3000` চলে যাবে — আর সেটা ঠিক করতে
  পুরো রি-বিল্ড লাগবে।
- **`AUTH_SECRET` কখনো `.env.example`-এর `change-me` রাখবেন না।** এটা দিয়েই
  অ্যাডমিন সেশন সাইন করা হয়। কেউ জেনে গেলে অ্যাডমিন প্যানেলে ঢুকে যেতে পারবে।
- URL-এর শেষে **স্ল্যাশ দেবেন না** — `https://mutibd.com/` নয়, `https://mutibd.com`।

---

## ৫. Persistent volume (ছবি হারিয়ে যাওয়া ঠেকাতে)

অ্যাপের **Storages** ট্যাব → **+ Add** → **Volume Mount**:

| ফিল্ড            | মান            |
| ---------------- | -------------- |
| Name             | `muti-uploads` |
| Destination Path | `/app/uploads` |

> ⚠️ **এই ধাপটা বাদ দেবেন না।** অ্যাডমিন থেকে আপলোড করা প্রতিটা ছবি ও PDF এখানে
> জমা হয়। ভলিউম না থাকলে প্রতিবার নতুন ডিপ্লয়ে **সব আপলোড মুছে যাবে** — লোগো,
> ফেভিকন, হিরো ছবি, গ্যালারি, সব।

---

## ৬. Health check

অ্যাপের **Health Checks** সেকশনে:

| ফিল্ড        | মান           |
| ------------ | ------------- |
| Enabled      | চালু          |
| Path         | `/api/health` |
| Port         | `3000`        |
| Interval     | `30` সেকেন্ড  |
| Timeout      | `5` সেকেন্ড   |
| Retries      | `3`           |
| Start period | `40` সেকেন্ড  |

এই এন্ডপয়েন্টটা ডাটাবেসেও একটা কুয়েরি চালায়, তাই ডাটাবেস ডাউন হলে কন্টেইনার
"unhealthy" দেখাবে — ভাঙা পেজ সার্ভ করবে না।

---

## ৭. ডোমেইন বসানো

অ্যাপের **General** ট্যাব → **Domains** ঘরে লিখুন:

```
https://mutibd.com
```

(`www` ও চাইলে কমা দিয়ে দুটোই দিতে পারেন, Coolify দুটোর জন্যই সার্টিফিকেট নেবে।)

---

## ৮. প্রথম ডিপ্লয়

**Deploy** চাপুন। লগ দেখুন — প্রথমবার ৫–১০ মিনিট লাগতে পারে।

ঠিকঠাক চললে লগের শেষে এই তিনটা লাইন দেখবেন:

```
==> Applying database migrations
==> Running seed
==> Starting Next.js
```

মাইগ্রেশন আর সিড **নিজে থেকেই** চলে — আপনাকে আলাদা করে কিছু চালাতে হবে না। সিডটা
বুদ্ধিমান: প্রতিবার শুধু অ্যাডমিন ইউজার আর সাইট সেটিংস আছে কি না দেখে, আর কোর্স-নোটিশ
তখনই বসায় যখন ডাটাবেস একদম খালি। তাই **রি-ডিপ্লয় করলে অফিস যা মুছেছে তা ফিরে আসে না**।

এখন `https://mutibd.com` খুলে দেখুন — HTTPS-এ সাইট আসার কথা।

---

## ৯. Cloudflare প্রক্সি চালু করা

সাইট HTTPS-এ আসার পর (অর্থাৎ Let's Encrypt সার্টিফিকেট পাওয়া হয়ে গেছে):

১. Cloudflare → **SSL/TLS** → encryption mode **Full (strict)** করুন।

- ❌ **Flexible করবেন না** — রিডাইরেক্ট লুপ হবে।
  ২. Cloudflare → **DNS** → দুটো A রেকর্ডের মেঘ **কমলা (Proxied)** করে দিন।
  ৩. Cloudflare → **SSL/TLS → Edge Certificates** → **Always Use HTTPS** চালু করুন।
  ৪. Cloudflare → **Speed → Optimization** → **Rocket Loader বন্ধ রাখুন**।

> Rocket Loader জাভাস্ক্রিপ্ট লোডের ক্রম বদলে দেয়, এতে Next.js-এর হাইড্রেশন ভাঙে —
> মোবাইল মেনু, ফর্ম, সিট কাউন্টার কাজ করা বন্ধ করে দিতে পারে।

---

## ১০. লাইভ হওয়ার পর প্রথম যে কাজগুলো

১. `https://mutibd.com/admin` — `ADMIN_EMAIL` ও `ADMIN_PASSWORD` দিয়ে লগইন করুন।
২. **সাথে সাথে পাসওয়ার্ড বদলান:** অ্যাডমিন → ইউজার → নিজের অ্যাকাউন্ট।
(এনভায়রনমেন্ট ভ্যারিয়েবলে লেখা পাসওয়ার্ড অনেকের চোখে পড়তে পারে।)
৩. **সাইট সেটিংস → ব্র্যান্ডিং** — আসল লোগো ও ফেভিকন আপলোড করুন (HANDOVER ১.৭, ১.৮)।
৪. `HANDOVER.md`-এর সেকশন ২ ধরে বাকি কনটেন্ট ভরুন — শিক্ষকমণ্ডলী, ছবি, ব্যাচের
তারিখ, আসনসংখ্যা, রুটিন ইত্যাদি।
৫. একটা টেস্ট আবেদন জমা দিয়ে দেখুন অ্যাডমিনের ইনবক্সে আসছে কি না এবং
নোটিফিকেশন ইমেইল যাচ্ছে কি না।

---

## ১১. ব্যাকআপ (অবশ্যই সেট করুন)

Coolify-র Postgres সার্ভিসে **Backups** ট্যাব আছে:

১. **+ Add backup** → schedule `0 3 * * *` (প্রতিদিন রাত ৩টা)।
২. **Number of backups to keep: 14** (স্পেক অনুযায়ী)।
৩. সম্ভব হলে একটা S3 বাকেট যুক্ত করুন — শুধু একই সার্ভারে ব্যাকআপ রাখলে সার্ভার
নষ্ট হলে ব্যাকআপও হারাবে।

**আপলোড ভলিউমের ব্যাকআপ আলাদা।** ডাটাবেস ব্যাকআপে ছবিগুলো থাকে না। সার্ভারে
একটা ক্রন রাখুন:

```sh
tar czf /backups/uploads-$(date +%F).tar.gz /var/lib/docker/volumes/<volume-name>/_data
```

---

## ১২. পরের বার আপডেট করতে

```bash
git push origin main
```

তারপর Coolify-তে **Redeploy** চাপুন। অথবা একবারের জন্য অ্যাপের
**Webhooks/General** সেকশনে **Auto Deploy** চালু করে দিন — তাহলে প্রতিটা push-এ
নিজে থেকেই ডিপ্লয় হবে।

মাইগ্রেশন আর সিড প্রতিবার নিজে থেকেই চলে, তাই স্কিমা বদলালেও আলাদা কিছু করতে হবে না।

---

## ১৩. সমস্যা হলে

| যা দেখছেন                                           | কারণ ও সমাধান                                                                                                                                   |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| বিল্ড মাঝপথে থেমে যায়, "killed" লেখা               | সার্ভারে RAM কম। VPS ৪ GB করুন, অথবা ২ GB swap যোগ করুন।                                                                                        |
| `Can't reach database server`                       | `DATABASE_URL` ভুল, অথবা অ্যাপ ও ডাটাবেস এক নেটওয়ার্কে নেই। অ্যাপের **Advanced** → **Connect to Predefined Network** চালু করে রি-ডিপ্লয় করুন। |
| সাইট খোলে কিন্তু SSL এরর                            | Cloudflare-এর মেঘ কমলা করার আগেই সার্টিফিকেট আসেনি। মেঘ ধূসর করুন → রি-ডিপ্লয় → সার্টিফিকেট এলে আবার কমলা করুন।                                |
| রিডাইরেক্ট লুপ (`ERR_TOO_MANY_REDIRECTS`)           | Cloudflare SSL mode **Flexible** আছে। **Full (strict)** করুন।                                                                                   |
| ডিপ্লয়ের পর সব ছবি উধাও                            | `/app/uploads` ভলিউম যোগ করা হয়নি (ধাপ ৫)। যোগ করুন — তবে আগের ছবিগুলো ফিরবে না, আবার আপলোড করতে হবে।                                          |
| সাইটম্যাপে `localhost:3000`                         | `NEXT_PUBLIC_SITE_URL`-এ "Build Variable" টিক দেওয়া হয়নি। টিক দিয়ে **Redeploy** (রিস্টার্ট নয়, পুরো রি-বিল্ড) করুন।                         |
| অ্যাডমিনে লগইন হয় না / সাথে সাথে লগআউট             | `AUTH_SECRET` বদলে গেছে, অথবা `AUTH_URL` আসল ডোমেইনের সাথে মেলে না। দুটো ঠিক করে রি-ডিপ্লয় করুন।                                               |
| আবেদন জমা হয় কিন্তু ইমেইল আসে না                   | `SMTP_PASS` খালি বা ভুল। Gmail-এর **App Password** লাগবে, সাধারণ পাসওয়ার্ড নয়। আবেদনগুলো কিন্তু অ্যাডমিনে জমা থাকছে।                          |
| মোবাইল মেনু/ফর্ম কাজ করে না                         | Cloudflare **Rocket Loader** বন্ধ করুন।                                                                                                         |
| অ্যাডমিনে সেভ করলে "Invalid Server Actions request" | প্রক্সির হেডার সমস্যা। `next.config.ts`-এ `experimental.serverActions.allowedOrigins: ["mutibd.com"]` যোগ করে রি-ডিপ্লয় করুন।                  |

---

## ১৪. চেকলিস্ট

লাইভ করার আগে একবার মিলিয়ে নিন:

- [ ] ডোমেইন কেনা ও Cloudflare-এ nameserver বসানো
- [ ] Postgres 16 সার্ভিস চালু, internal URL কপি করা
- [ ] Build Pack = **Dockerfile**, port 3000
- [ ] ১২টা environment variable বসানো
- [ ] `NEXT_PUBLIC_SITE_URL`-এ **Build Variable** টিক দেওয়া
- [ ] `AUTH_SECRET` নতুন করে জেনারেট করা (`change-me` নয়)
- [ ] `/app/uploads` ভলিউম যোগ করা
- [ ] Health check `/api/health`
- [ ] ডোমেইন বসানো ও HTTPS কাজ করছে
- [ ] Cloudflare: Full (strict), Always Use HTTPS, Rocket Loader বন্ধ
- [ ] অ্যাডমিন পাসওয়ার্ড বদলানো
- [ ] নাইটলি ব্যাকআপ, ১৪ দিন
- [ ] একটা টেস্ট আবেদন করে দেখা
